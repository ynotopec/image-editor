[project]
name = "image-editor"
version = "0.1.0"
description = "FastAPI image editing API and UI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pillow>=10.4.0",
  "python-multipart>=0.0.9",
  "requests>=2.32.0",
  "diffusers>=0.32.0",
  "transformers>=4.44.0",
  "accelerate>=0.33.0",
  "torch>=2.4.0",
]

[tool.uv]
package = false
